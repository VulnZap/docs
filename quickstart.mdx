---
title: "Quick Start"
description: "Install and scan in under 5 minutes"
icon: "rocket"
---

## Installation

<CodeGroup>

```bash npx
npx vulnzap init
```

```bash npm
npm i -g vulnzap && npx vulnzap init
```

```bash yarn
yarn global add vulnzap
```

</CodeGroup>

## Setup

### 1. Authenticate

Set your API key as an environment variable:

```bash
export VULNZAP_API_KEY=your_api_key_here
```

Get your API key from [vulnzap.com/dashboard](https://vulnzap.com/dashboard/api-keys)

<Tip>
  Your API key should be stored as an environment variable and never committed to your repository.
</Tip>

### 2. Connect Your Repository

Navigate to your project directory:

```bash
cd /path/to/your/project
```

Ensure your `VULNZAP_API_KEY` environment variable is set

### 3. Run Your First Scan

#### Option A: Scan Entire Repository

```bash
vulnzap scan
```

This performs a deep sweep of your entire codebase, ranking findings by exploitability.

#### Option B: Scan Specific Files

```bash
vulnzap scan src/auth.js src/db/queries.ts
```

#### Option C: Watch Mode (Inline Scanning)

```bash
vulnzap watch
```

Automatically scans files as you save them during development.

## View Results

### In the Terminal

Scan results appear immediately in your terminal:

```bash
╔══════════════════════════════════════════════════════════╗
║ VulnZap Scan Results                                      ║
╠══════════════════════════════════════════════════════════╣
║ Risk Score: 67 (Medium)                                   ║
║ Total Findings: 8                                         ║
║   • Critical: 2                                           ║
║   • High: 3                                               ║
║   • Medium: 2                                             ║
║   • Low: 1                                                ║
╚══════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────┐
│ CRITICAL: SQL Injection                                  │
├─────────────────────────────────────────────────────────┤
│ File: src/db/queries.ts:45                              │
│ Issue: Unsanitized user input in SQL query              │
│ Fix Available: Yes                                       │
└─────────────────────────────────────────────────────────┘
```

### In the Dashboard

Visit [vulnzap.com/dashboard](https://vulnzap.com/dashboard) to see:
- Visual risk scoring and trends
- Detailed vulnerability breakdowns
- One-click patch application
- Historical scan data

## Apply Fixes

### Automatic Patching

Apply all recommended fixes at once:

```bash
vulnzap fix --all
```

### Selective Patching

Review and apply specific fixes:

```bash
vulnzap fix --interactive
```

This opens an interactive prompt where you can:
- Review each vulnerability
- See the proposed patch
- Accept, reject, or modify fixes

### Manual Review

Export patches for manual review:

```bash
vulnzap fix --export patches/
```

<Warning>
  Always review patches before applying them to production code. While VulnZap patches are deterministic and context-aware, understanding the changes ensures they align with your application logic.
</Warning>

## IDE Integration

For real-time scanning as you code, integrate VulnZap with your IDE:

<Tabs>
  <Tab title="Cursor">
    1. Create `.cursor/mcp.json` in your project root
    2. Add VulnZap configuration:
    ```json
    {
      "mcpServers": {
        "VulnZap": {
          "command": "vulnzap",
          "args": ["secure", "--ide", "cursor"],
          "env": {
            "VULNZAP_API_KEY": "your_api_key_here"
          }
        }
      }
    }
    ```
    3. Restart Cursor
  </Tab>
  
  <Tab title="Windsurf">
    1. Create `.codeium/windsurf/mcp_config.json` in your project root
    2. Add VulnZap configuration:
    ```json
    {
      "mcpServers": {
        "VulnZap": {
          "command": "vulnzap",
          "args": ["secure", "--ide", "windsurf"],
          "env": {
            "VULNZAP_API_KEY": "your_api_key_here"
          }
        }
      }
    }
    ```
    3. Restart Windsurf
  </Tab>
  
  <Tab title="Cline">
    Configure in Cline settings with your VulnZap API key
  </Tab>
</Tabs>

<Check>
  Once configured, VulnZap will automatically scan AI-generated code and highlight vulnerabilities inline.
</Check>

## CI/CD Integration

### GitHub Actions

Add VulnZap to your GitHub Actions workflow:

```yaml .github/workflows/vulnzap.yml
name: VulnZap Security Scan

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main]

jobs:
  security-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Run VulnZap Scan
        uses: vulnzap/scan-action@v1
        with:
          api-key: ${{ secrets.VULNZAP_API_KEY }}
          fail-on: high # Block PRs with high/critical findings
          
      - name: Upload Results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: vulnzap-results
          path: vulnzap-report.json
```

<Tip>
  Get your API key from [vulnzap.com/dashboard](https://vulnzap.com/dashboard) and add it to GitHub Secrets at **Settings** → **Secrets and variables** → **Actions**
</Tip>

### GitLab CI

```yaml .gitlab-ci.yml
vulnzap:
  stage: security
  image: node:18
  before_script:
    - npm install -g vulnzap
  script:
    - vulnzap scan --format json > vulnzap-report.json
  artifacts:
    reports:
      vulnzap: vulnzap-report.json
  only:
    - merge_requests
    - main
```

## Next Steps

<CardGroup cols={2}>
  <Card
    title="Dashboard Guide"
    icon="chart-line"
    href="/features/dashboard"
  >
    Explore the full dashboard capabilities
  </Card>
  
  <Card
    title="Security Coverage"
    icon="shield-check"
    href="/features/security-coverage"
  >
    Learn what vulnerabilities VulnZap detects
  </Card>
  
  <Card
    title="CLI Reference"
    icon="terminal"
    href="/integration/cli"
  >
    Master the command-line interface
  </Card>
  
  <Card
    title="API Documentation"
    icon="code"
    href="/api-reference/introduction"
  >
    Integrate VulnZap programmatically
  </Card>
</CardGroup>

## Troubleshooting

<AccordionGroup>
  <Accordion title="Authentication Failed">
    If authentication fails:
    1. Ensure you're connected to the internet
    2. Check that your API key is valid at [vulnzap.com/dashboard](https://vulnzap.com/dashboard) → Settings → API Keys
    3. Verify your `VULNZAP_API_KEY` environment variable is set correctly
  </Accordion>
  
  <Accordion title="No Vulnerabilities Found">
    If scans return no results:
    - Verify your language is supported (JS, TS, Python)
    - Check that files aren't ignored in `.vulnzapignore`
    - Run with `--verbose` flag for detailed logging
  </Accordion>
  
  <Accordion title="IDE Integration Not Working">
    For IDE integration issues:
    1. Verify your MCP configuration file exists (`.cursor/mcp.json` or `.codeium/windsurf/mcp_config.json`)
    2. Check that VULNZAP_API_KEY is set in the configuration
    3. Restart your IDE after configuration changes
    4. See [IDE Integration](/features/ide-integration) for detailed setup
  </Accordion>
</AccordionGroup>

<Card
  title="Still need help?"
  icon="life-ring"
  href="mailto:support@vulnzap.com"
>
  Contact our support team at support@vulnzap.com
</Card>
